<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE-edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">
    <title>XProg home page</title>
</head>
<body>
    <div class="container">
        <h1>XProgramming.com</h1>
        <h2>Favorites</h2>

        <div class="row">
            <% @favorites.each do |s| %>
                <div class="rj-article-box col-md-4 col-sm-6">
                    <h3><%= s.category %></h3>
                    <div class="row">
                        <div class="col-md-4">
                            <img src="<%= s.icon %>" width="75px%"/>
                        </div>
                        <div class="col-md-8">
                            <h4><%= s.title %></h4>
                            <%= s.blurb %>
                        </div>
                    </div>
                </div>
            <% end %>
        </div>
    </div>

        <h2>Recent Articles</h2>

    <div class="container">
        <!-- <div style="padding: 0em 5em;"> -->
        <div class="row">
            <% i = 0 %>
            <% @the_rest.each do |s| %>
                <% if i%4 == 0 then %>
                    <div class="row">
                <%end%>
                        <div class="col-lg-2 col-md-3 col-sm-6 col-xs-12">
                            <img style="float:left; " src="<%= s.icon%>" width="75px%"/>
                                <h3><%= s.title%></h3>
                                <h4>(<%=s.category%>)</h4>
                            <%= s.blurb %>
                        </div>
                <% i = i + 1 %>
                <% if i%4 == 0 then %>
                    </div> 
                <% end %>
            <% end %>
        </end>
    </div>

        <hr/>
        <p>This is at the bottom of the page</p>
</body>
</html>
